Buenos días, 

lo único que hay que modificar para que funcione el código es cambiar la ruta. Para
cambiarla teneis que poner la ruta de la carpeta donde esta el ws.py. Al abrir el ws.py,
en la linea 479, donde pone C:/python3 cambiarlo por vuestra ruta del archivo.

Cuando se ejecute el ws.py, tan solo habra que entrar en el navegador y escribir localhost:8888
para que funcione el programa.

La secuencia de botones es, insertar, leer json e iniciar.

Un saludo